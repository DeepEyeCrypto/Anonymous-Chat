package com.phantomnet.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── v2.0 Design System: "Obsidian & Emerald" ──

// Backgrounds
val Background = Color(0xFF0B0E11)
val Surface = Color(0xFF1C1F26)
val SurfaceVariant = Color(0xFF2D333B)

// Primary (Emerald)
val PhantomGreen = Color(0xFF00E676)
val PhantomGreenPressed = Color(0xFF00C853)

// Secondary
val SecureBlue = Color(0xFF58A6FF)

// Accent
val TorOnion = Color(0xFF7D4698)

// Text
val TextPrimary = Color(0xFFC9D1D9)
val TextHeading = Color(0xFFFFFFFF)
val TextMuted = Color(0xFF8B949E)

// Semantic
val ErrorRed = Color(0xFFFF5252)
val WarningYellow = Color(0xFFFFD600)
val Divider = Color(0xFF30363D)

// Glass
val GlassWhite = Color(0x1AFFFFFF)

// ── Legacy aliases (for existing screens that reference the old names) ──
val HackerGreen = PhantomGreen
val DarkBackground = Background
val EncryptedText = TextPrimary
val SignalPurple = Color(0xFF3F3BBD)
